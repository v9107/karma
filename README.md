# Simple todo cli made with rust

## Playing around with hexagonal architecture.


### 🔔 Note 
Currently for the database I'm using a simple `Vec<T>` which works as in-memory db for now,
will move to proper database
